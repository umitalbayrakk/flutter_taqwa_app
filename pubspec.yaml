name: flutter_taqwa_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.7.2
  google_nav_bar: ^5.0.7
  flutter_svg: ^2.2.0
  provider: ^6.1.5
  flutter_map: ^8.1.1
  latlong2: ^0.9.1
  http: ^1.5.0-beta.2
  geolocator: ^14.0.2
  url_launcher: ^6.3.2
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  flutter_animate: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/esmaul_husna.json
    - assets/hadith.json
